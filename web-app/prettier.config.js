// Prettier configuration for web-app extending shared config
import sharedConfig from '@my-affirms/shared/config/prettier.config.js'

export default {
  ...sharedConfig,
  // Web-app specific overrides if needed
}